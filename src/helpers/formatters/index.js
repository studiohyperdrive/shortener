import { format } from "./format";

import { trimSlashesFormatter } from "./trimSlashesFormatter";
import { urlFormatter } from "./urlFormatter";
import { targetFormatter } from "./targetFormatter";

export {
	trimSlashesFormatter as trimSlashes,
	urlFormatter as url,
	targetFormatter as target,
};

export default format;
